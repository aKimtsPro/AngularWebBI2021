import { ShowLivreDirective } from './show-livre.directive';

describe('ShowLivreDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowLivreDirective();
    expect(directive).toBeTruthy();
  });
});
